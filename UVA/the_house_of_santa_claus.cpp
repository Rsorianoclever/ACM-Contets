#include<iostrea>
#include<vector>
#include<queue>
using namespace std;
int A[100][100];
vector<int>color
void dfs(int nodo){
    color[nodo]=1;
    for(int i=0;i<n;i++){
        if(A[nodo][i] && color[i]==0)
            dfs(i);}
    color[nodo]=2;
}
int main(){
    int n;
    cin>>n;
    while(n--){
        memset(A,0,sizeof A);
        int A,B,m,;
        cin>>A>>B;  
        while(cin)
    }
    return 0;
}
