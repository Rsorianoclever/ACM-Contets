#include<iostream>
using namespace  std;
int main()
{	
	int n; 
	string s;
	while(cin>>s)
	{
		n=s.size();
		bool mayuscula = true  
		for(i=0;i<n;i++)
		{
			if('A'=<s[i] && s[i]=<'Z')
				continue;
				//s[i]=s[i]+'a'-'A';	
			else mayuscula false; break;
		}
		if(mayuscula)
		{
			for (i=0;i<n;i++)
				s[i]=s[i]+'a'-'A';
		}	
		cout << s << endl;	
		else 
			if('a'<=s[0] && s[0}=<'z')
			 	if (
					for(i;=1i<=n;i++)
				  )
	}
	
}	
